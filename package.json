{
  "name": "fivem-store",
  "version": "1.0.0",
  "description": "FiveM Store with GitHub API",
  "scripts": {
    "start": "node -r dotenv/config server.js",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
} 